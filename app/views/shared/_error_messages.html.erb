<% if target.errors.any? %>
<div id="error-explanation">
  <h2><%= pluralize(target.errors.count, "error") %> record could not be saved:</h2>
  <ul>
  <% target.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
</div>
<% end %>